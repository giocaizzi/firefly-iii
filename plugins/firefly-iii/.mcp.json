{
  "mcpServers": {
    "firefly-iii": {
      "type": "http",
      "url": "${user_config.firefly_url}/api/v1/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.firefly_token}",
        "Accept": "application/json, text/event-stream"
      }
    }
  }
}
